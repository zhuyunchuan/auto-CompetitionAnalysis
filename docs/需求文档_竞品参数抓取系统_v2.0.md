# 竞品参数自动抓取与竞品分析系统 - 需求文档 v2.0

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 文档版本 | v2.0 |
| 日期 | 2026-05-29 |
| 项目名称 | auto-CompetitionAnalysis |
| 仓库地址 | https://github.com/zhuyunchuan/auto-CompetitionAnalysis |
| 定位 | 竞品网络摄像机产品参数自动抓取与竞品分析系统 |
| 前序文档 | `docs/需求文档_竞品参数抓取系统_v1.0.md` |

---

## 2. 背景与目标

### 2.1 背景

当前需要持续跟踪 Hikvision（海康威视）与 Dahua（大华）两大品牌的网络摄像机产品参数，并沉淀为结构化数据，支持后续人工分析与策略研判。在完成基础数据采集后，需要进一步对两家品牌的产品进行跨品牌对标与参数差异分析，以支撑产品规划与竞争策略决策。

### 2.2 目标

1. **数据采集**：周期性抓取两家官网目标产品的完整层级与参数信息。
2. **结构化输出**：将抓取结果标准化并输出为可直接使用的 Excel 和 CSV 宽表。
3. **跨品牌对标**：自动匹配 Hikvision 与 Dahua 的同类产品型号，建立对标关系。
4. **参数差异分析**：对对标型号进行关键参数逐项对比，识别差异与优劣势。
5. **分析报告生成**：自动生成竞品分析报告，涵盖对标总览、参数差异明细和系列覆盖度。
6. **人工可介入**：支持人工补录、复核与修订，形成可持续迭代的数据资产。

### 2.3 核心原则

1. **先爬虫、再分析**：系统分为数据采集（Phase 1）和竞品分析（Phase 2）两个独立阶段，采集先行、分析后续。
2. **爬虫与分析解耦**：采集层（Node.js Playwright）与分析层（Python Pipeline）相互独立，各自可独立迭代和部署。
3. **两阶段可独立运行**：Phase 1 可独立产出数据资产（CSV/JSONL），Phase 2 可基于已有数据随时运行分析。
4. **层级优先**：每个产品必须绑定 `brand → series_l1 → series_l2 → model` 完整路径。
5. **页面即真相**：系列/子系列以官网页面当期可见结构为准，不依赖静态字典。
6. **人工可介入**：人工补录和修订优先级高于机器抽取。

---

## 3. 覆盖范围

### 3.1 品牌覆盖

| 品牌 | 系列 | 子系列数 | 型号数 |
|------|------|:--------:|:-----:|
| **Hikvision** | Pro 系列 (EasyIP) | 8 | ~410 |
| | Value 系列 | 4 | ~158 |
| | HiLook 系列 | 1 | ~30 |
| **Dahua** | WizSense 2 | 4 | 107 |
| | WizSense 3 | 7 | 85 |

### 3.2 In Scope

**Phase 1 — 数据采集：**

1. Hikvision Pro / Value / HiLook 系列的层级发现与规格抓取。
2. Dahua WizSense 2 / WizSense 3 系列的层级发现与规格抓取。
3. 官网参数抽取、标准化、单位归一化。
4. CSV 长表（每行一个 field-value 对）输出。
5. CSV 宽表（每行一个产品，列为参数字段）透视输出。
6. 人工补录与修订回写。

**Phase 2 — 竞品分析：**

7. 跨品牌型号对标匹配：基于参数相似度自动匹配 Hikvision 与 Dahua 同类产品。
8. 参数差异分析：对对标型号进行关键参数逐项对比，标注差异类型。
9. 竞品分析报告生成：自动输出 Excel 多 Sheet 分析报告。

### 3.3 Out of Scope

1. 大模型辅助策略分析（后续 Phase 4 接入）。
2. 非网络摄像机品类（如 NVR、门禁、报警等）。

---

## 4. 数据源与抓取策略

### 4.1 Hikvision

#### 4.1.1 Pro 系列

| 属性 | 内容 |
|------|------|
| 页面 URL | `https://www.hikvision.com/en/products/IP-Products/Network-Cameras/Pro-Series-EasyIP-/` |
| 子系列数 | 8 |
| 子系列列表 | EasyIP 4.0 with ColorVu, EasyIP 4.0 with AcuSense, EasyIP 4.0 Plus with ColorVu, EasyIP 4.0 Plus with AcuSense, EasyIP 3.0, EasyIP 2.0 Plus, EasyIP 2.0 Plus with AcuSense, EasyIP 1.0 Plus |
| 数据来源 | AEM `search_list_copy.json` API（非 `search_list.json`） |

**子系列分组方式**：通过 `page.evaluate()` 在浏览器上下文中 fetch API 返回的 JSON 数据，按 `selectParameters.Subseries[0]` 字段分组。

#### 4.1.2 Value 系列

| 属性 | 内容 |
|------|------|
| 页面 URL | `https://www.hikvision.com/en/products/IP-Products/Network-Cameras/value-series/` |
| 子系列数 | 4 |
| 子系列列表 | Value Series with ColorVu 3.0 & MD 3.0, Value Series with MD 2.0, Value Series with ColorVu & MD 2.0, Value Series Essential |
| 数据来源 | AEM `search_list.json` API |

#### 4.1.3 HiLook 系列

| 属性 | 内容 |
|------|------|
| 页面 URL | `https://www.hikvision.com/en/products/HiLook-IP-Products/Network-Cameras/Value-Camera/` |
| 子系列数 | 1（Value Camera） |
| 数据来源 | 页面 DOM 直接提取 |

#### 4.1.4 规格提取方式

访问每个产品详情页，从 `.tech-specs-accordion-container` 容器下的规格标签提取，回退到 `<table>` 解析。

### 4.2 Dahua

#### 4.2.1 系列入口

| 属性 | 内容 |
|------|------|
| WizSense 2 | `https://www.dahuasecurity.com/products/network-products/network-cameras/wizsense-2-series` |
| WizSense 3 | `https://www.dahuasecurity.com/products/network-products/network-cameras/wizsense-3-series` |

#### 4.2.2 WizSense 2 子系列

| # | 子系列 | 型号数 | 说明 |
|---|--------|:-----:|------|
| 1 | WizColor | 17 | 彩色夜视固定焦距 |
| 2 | Smart Dual Light | 48 | 智能双光 |
| 3 | IR | 40 | 红外 |
| 4 | 4G Camera | 2 | 4G 无线（之前被硬编码过滤遗漏） |

#### 4.2.3 WizSense 3 子系列

| # | 子系列 | 型号数 | 说明 |
|---|--------|:-----:|------|
| 1 | TiOC PRO-WizColor | 15 | 全彩+音频报警 Pro |
| 2 | Smart Dual Light | 36 | 智能双光 |
| 3 | TiOC | 8 | 音频报警 |
| 4 | IR | 2 | 红外 |
| 5 | Anti-Corrosion | 19 | 防腐系列 |
| 6 | 4G Camera | 4 | 4G 无线 |
| 7 | WizSense Camera | 1 | 通用 WizSense |

#### 4.2.4 子系列发现方式

页面通过 `div.tabs-li` 元素展示子系列 tab。完整发现所有 tab 标签文本，逐个点击遍历并翻页拉取全部型号。

#### 4.2.5 规格提取方式

访问每个产品详情页，点击"Specifications"标签后从 `<table>` 提取参数。

---

## 5. 技术架构

### 5.1 整体架构

系统采用**双层解耦架构**，数据采集层（Node.js Playwright）与竞品分析层（Python Pipeline）相互独立，通过 CSV/JSONL 数据文件进行衔接。

```
+---------------------------------------------------------------+
|              Phase 1: 数据采集层 (Node.js Playwright)          |
|                                                                |
|  结构发现脚本 → 批量抓取脚本 → 宽表转换脚本                    |
|  统一入口: run.mjs / run.bat                                   |
|                                                                |
|  +------------------+  +------------------+                    |
|  | Hikvision Adapter|  |  Dahua Adapter   |                    |
|  | (Pro/Value/HiLook)|  | (WizSense 2/3)  |                    |
|  +------------------+  +------------------+                    |
+---------------------------------------------------------------+
                    |
                    | CSV 长表 / 宽表 / JSONL 数据交付
                    v
+---------------------------------------------------------------+
|              Phase 2: 竞品分析层 (Python Pipeline)             |
|                                                                |
|  数据导入 → 标准化 → 对标匹配 → 差异分析 → 报告导出           |
|  统一入口: src/pipeline/                                       |
|                                                                |
|  +----------+ +----------+ +---------+ +--------+ +------+    |
|  | Storage  | | Extractor| | Quality | | Export | |Manual|    |
|  | (SQLite) | | (归一化) | | (检测)  | | (Excel)| |(补录)|    |
|  +----------+ +----------+ +---------+ +--------+ +------+    |
+---------------------------------------------------------------+
```

### 5.2 技术栈

| 组件 | 技术选型 | 用途 | 所属阶段 |
|------|---------|------|:--------:|
| 浏览器自动化 | Playwright (Chromium) | 页面抓取、JS 渲染、反爬绕过 | Phase 1 |
| 数据采集 | Node.js (ESM) | 结构发现 + 批量规格抓取 | Phase 1 |
| 数据处理 | Python 3.13 | 宽表转换、标准化、分析 | Phase 1+2 |
| 数据存储 | CSV + JSONL | 长表/宽表/元数据/进度文件 | Phase 1 |
| 数据存储 | SQLite + Parquet | 结构化存储、批次快照 | Phase 2 |
| 报表导出 | openpyxl | Excel 多 Sheet 报告 | Phase 2 |
| 版本控制 | Git + GitHub | 代码管理 | 全局 |

### 5.3 关键脚本

**数据采集脚本（Node.js）：**

| 脚本 | 功能 |
|------|------|
| `run.mjs` | 统一 CLI 入口，支持 8 个子命令（all / discover / specs / wide / check） |
| `hikvision_structure_by_filter.mjs` | Hikvision 层级发现（Pro/Value API 分组，HiLook DOM 提取） |
| `dahua_wizsense_structure.mjs` | Dahua WizSense 2/3 层级发现（tab 点击 + 翻页） |
| `hikvision_batch_specs_from_structure.mjs` | Hikvision 全量规格批量抓取（4 并发，断点续爬） |
| `dahua_batch_specs_from_structure.mjs` | Dahua 全量规格批量抓取（4 并发，断点续爬） |
| `normalize_specs.py` | 长表→宽表透视转换 |

**分析处理模块（Python）：**

| 模块 | 功能 |
|------|------|
| `src/core/` | 核心类型定义、常量、配置管理 |
| `src/storage/` | SQLite 数据库层、Repository 模式、Parquet 存储 |
| `src/adapters/` | 品牌适配器（Hikvision/Dahua 接口抽象） |
| `src/crawler/` | HTTP 客户端、页面抓取器、层级发现编排 |
| `src/extractor/` | 字段注册表、规格提取器、归一化器、解析器 |
| `src/quality/` | 质量规则定义、异常检测引擎（6 种 issue 类型） |
| `src/pipeline/` | OpenClaw DAG 任务编排（9 个任务节点） |
| `src/export/` | Excel 生成器、运行摘要追踪器 |
| `src/manual/` | 手动输入导入器、覆盖服务（审计追踪） |
| `src/mappings/` | 字段别名映射、单位转换规则 |

---

## 6. Python Pipeline 架构

### 6.1 概述

Python Pipeline 是系统的分析层核心基础设施，采用 9-Agent 并行开发模式构建，共 53 个 Python 文件，约 13,715 行代码。在 Phase 2 中，它负责数据标准化、跨品牌对标分析、参数差异检测和报告导出。

### 6.2 模块架构

```
src/
├── core/                  # 核心基础设施
│   ├── config.py          # 配置管理
│   ├── logging.py         # 日志管理
│   ├── constants.py       # 常量定义（字段编码、严重级别、issue 类型）
│   └── types.py           # 冻结数据结构（dataclasses）
├── storage/               # 数据存储层
│   ├── db.py              # SQLite 数据库初始化
│   ├── schema.py          # 6 张表 Schema 定义
│   ├── repo_hierarchy.py  # 层级快照 Repository
│   ├── repo_catalog.py    # 产品目录 Repository
│   ├── repo_specs.py      # 产品参数 Repository
│   ├── repo_issues.py     # 数据质量 Issue Repository
│   ├── repo_run_summary.py# 运行摘要 Repository
│   └── parquet_store.py   # Parquet 批次快照存储
├── adapters/              # 品牌适配器
│   ├── base_adapter.py    # 通用接口抽象
│   ├── hikvision_adapter.py # Hikvision 适配器
│   └── dahua_adapter.py   # Dahua 适配器
├── crawler/               # 爬虫基础设施
│   ├── http_client.py     # HTTP 客户端（重试、限速、UA 轮换）
│   ├── page_fetcher.py    # 页面抓取器（缓存 + Playwright 回退）
│   ├── hierarchy_discovery.py  # 层级发现编排器
│   ├── catalog_collector.py    # 产品目录收集器
│   └── detail_collector.py     # 详情页并行抓取器
├── extractor/             # 提取器和归一化器
│   ├── field_registry.py  # 字段注册表（19 个 Phase 1 字段）
│   ├── spec_extractor.py  # 规格提取器（三层回退策略）
│   ├── normalizer.py      # 归一化器（单位转换、格式标准化）
│   └── parsers/           # 专用解析器
│       ├── resolution_parser.py  # 分辨率解析
│       ├── stream_parser.py      # 码流解析
│       └── range_parser.py       # 距离范围解析
├── quality/               # 数据质量检测
│   ├── issue_rules.py     # 质量规则（6 种 issue 类型）
│   └── issue_detector.py  # 检测引擎（批处理、跨记录检测）
├── pipeline/              # 任务编排
│   ├── dag.py             # 主 DAG 定义（任务依赖链）
│   ├── tasks_discover.py  # 层级发现任务
│   ├── tasks_collect.py   # 目录采集任务
│   ├── tasks_extract.py   # 参数提取任务
│   ├── tasks_quality.py   # 质量检测任务
│   ├── tasks_merge_manual.py  # 人工补录合并任务
│   └── tasks_export.py    # 报告导出任务
├── export/                # 报表导出
│   ├── excel_writer.py    # Excel 生成器（7 个 Sheet）
│   └── run_summary_writer.py  # 运行摘要写入器
├── manual/                # 人工输入处理
│   ├── manual_importer.py # Excel 批量导入
│   └── override_service.py# 覆盖服务（审计追踪）
└── mappings/              # 映射规则
    ├── field_alias.yaml   # 字段别名映射（中英文）
    └── unit_rules.yaml    # 单位转换规则
```

### 6.3 存储方案

| 存储类型 | 用途 | 说明 |
|---------|------|------|
| SQLite | 在线结构化数据 | 单文件部署，低运维成本，6 张核心表 |
| Parquet | 批次快照与分析副本 | 按 `run_id` 分目录存储 |
| Excel | 报表交付 | 固定 Sheet 结构，人工可直接使用 |

### 6.4 DAG 任务链

```
initialize_run → discover_hierarchy → crawl_product_catalog →
fetch_product_detail → extract_and_normalize_specs →
merge_manual_inputs → detect_data_quality_issues →
export_excel_report → notify_run_summary
```

每个任务节点具备幂等性和可重试性，支持失败后从断点继续执行。

### 6.5 在 Phase 2 中的角色

Python Pipeline 在 Phase 2 竞品分析中承担以下职责：

1. **数据导入**：读取 Phase 1 产出的 CSV/JSONL 数据，导入 SQLite 数据库。
2. **数据标准化**：通过字段注册表和归一化器，将两家品牌的参数统一到相同字段编码和单位体系。
3. **对标匹配**：基于标准化后的参数，计算跨品牌型号相似度，建立对标关系。
4. **差异分析**：对对标型号进行参数逐项对比，标注差异类型和严重级别。
5. **报告导出**：生成 Excel 多 Sheet 竞品分析报告。

---

## 7. Phase 2: 竞品分析需求

### 7.1 跨品牌型号对标匹配

#### 7.1.1 目标

自动匹配 Hikvision 与 Dahua 的同类产品型号，建立跨品牌对标关系，为参数差异分析提供基础。

#### 7.1.2 对标维度

| 维度 | 字段 | 权重 | 说明 |
|------|------|:----:|------|
| 分辨率 | `max_resolution` | 高 | 主流：2MP/4MP/5MP/8MP |
| 镜头类型 | `lens_type` | 高 | 固定/变焦/鱼眼 |
| 补光方式 | `supplement_light_type` | 中 | ColorVu/IR/双光/无补光 |
| 码流数 | `stream_count` | 中 | 1/2/3 路 |
| AI 功能 | `deep_learning_function_categories` | 中 | 人脸/车辆/行为分析等 |
| 防护等级 | `approval_protection` | 低 | IP67/IP66 等 |
| 光圈 | `aperture` | 低 | f/1.0 ~ f/2.8 |

#### 7.1.3 匹配策略

1. **参数相似度评分**：对每个对标维度计算归一化相似度分数（0-1），加权求和得到综合匹配分数。
2. **阈值过滤**：综合匹配分数 ≥ 0.7 的型号对保留为候选对标关系。
3. **一对多处理**：同一型号可匹配多个对标型号，按匹配分数降序排列。
4. **人工校正**：支持人工确认/拒绝/补充对标关系。

#### 7.1.4 输出

对标关系表（`model_mapping`）：

| 字段 | 类型 | 说明 |
|------|------|------|
| mapping_id | string | 对标关系唯一标识 |
| hikvision_model | string | Hikvision 型号 |
| hikvision_series_l1 | string | Hikvision 一级系列 |
| hikvision_subseries | string | Hikvision 子系列 |
| dahua_model | string | Dahua 型号 |
| dahua_series_l1 | string | Dahua 一级系列 |
| dahua_subseries | string | Dahua 子系列 |
| confidence_score | float | 匹配置信度（0-1） |
| match_dimensions | string | 匹配的维度列表 |
| is_manual_confirmed | boolean | 是否经人工确认 |
| created_at | datetime | 创建时间 |

### 7.2 参数差异分析

#### 7.2.1 目标

对已建立对标关系的型号对，进行关键参数逐项对比，识别参数差异、缺失和优劣势。

#### 7.2.2 分析维度

对所有标准化后的参数字段进行逐项对比，重点关注以下核心字段：

| 字段 | 对比方式 | 说明 |
|------|---------|------|
| `max_resolution` | 精确匹配 | 分辨率数值对比 |
| `lens_type` | 精确匹配 | 固定/变焦/焦距范围 |
| `aperture` | 数值对比 | 光圈数值大小 |
| `supplement_light_type` | 集合对比 | 补光方式是否一致 |
| `supplement_light_range` | 数值对比 | 补光距离（米） |
| `main_stream_max_fps_resolution` | 结构化对比 | FPS + 分辨率组合 |
| `stream_count` | 数值对比 | 码流数量 |
| `interface` | 集合对比 | 接口项差异 |
| `deep_learning_function_categories` | 集合对比 | AI 功能差异 |
| `approval_protection` | 精确匹配 | 防护等级 |
| `approval_anti_corrosion_protection` | 精确匹配 | 防腐等级 |

#### 7.2.3 差异类型

| 差异类型 | 标识 | 说明 |
|---------|------|------|
| 双方一致 | `match` | 参数值完全相同 |
| 数值差异 | `value_diff` | 数值型参数存在差异（标注差值） |
| 功能差异 | `feature_diff` | 集合型参数存在差异（标注独有项） |
| 单方缺失 | `one_side_missing` | 一方有值，另一方无值 |
| 双方缺失 | `both_missing` | 双方均无此参数 |

#### 7.2.4 输出

参数差异记录（`param_diff`）：

| 字段 | 类型 | 说明 |
|------|------|------|
| mapping_id | string | 关联的对标关系 ID |
| field_code | string | 参数字段编码 |
| field_name | string | 参数字段名称 |
| hikvision_value | string | Hikvision 参数值 |
| dahua_value | string | Dahua 参数值 |
| diff_type | string | 差异类型（match/value_diff/feature_diff/one_side_missing/both_missing） |
| diff_detail | string | 差异明细描述 |
| severity | string | 严重级别（P1/P2/P3） |
| created_at | datetime | 创建时间 |

### 7.3 竞品分析报告生成

#### 7.3.1 目标

基于对标关系和参数差异分析结果，自动生成结构化的竞品分析 Excel 报告，供产品规划和竞争策略决策使用。

#### 7.3.2 报告结构

生成的 Excel 报告包含以下 Sheet：

| Sheet | 名称 | 内容 |
|-------|------|------|
| 1 | 对标总览 | 品牌-系列-型号匹配概览，含匹配置信度 |
| 2 | 参数差异明细 | 逐型号逐参数对比，含差异类型和严重级别 |
| 3 | 系列覆盖度 | 各品牌系列布局对比（子系列数、型号数、功能覆盖） |
| 4 | 数据质量 | 异常数据标注（缺失字段、解析失败、单位异常等） |
| 5 | 运行摘要 | 分析运行统计（对标对数、差异统计、覆盖率等） |

#### 7.3.3 对标总览 Sheet

| 列 | 说明 |
|----|------|
| Hikvision 系列 | Pro / Value / HiLook |
| Hikvision 子系列 | EasyIP 4.0 with ColorVu 等 |
| Hikvision 型号 | DS-2CDxxxx |
| Dahua 系列 | WizSense 2 / WizSense 3 |
| Dahua 子系列 | WizColor / Smart Dual Light 等 |
| Dahua 型号 | IPC-Hxxxx |
| 匹配置信度 | 0.0 ~ 1.0 |
| 匹配维度 | 分辨率/镜头/补光等 |
| 差异参数数 | 存在差异的参数项数量 |
| 人工确认状态 | 待确认 / 已确认 / 已拒绝 |

#### 7.3.4 参数差异明细 Sheet

| 列 | 说明 |
|----|------|
| 对标 ID | mapping_id |
| Hikvision 型号 | DS-2CDxxxx |
| Dahua 型号 | IPC-Hxxxx |
| 参数字段 | 参数名称 |
| Hikvision 值 | 参数值 |
| Dahua 值 | 参数值 |
| 差异类型 | match / value_diff / feature_diff / one_side_missing |
| 差异明细 | 具体差异描述 |
| 严重级别 | P1（红色）/ P2（橙色）/ P3（黄色） |

#### 7.3.5 系列覆盖度 Sheet

| 列 | 说明 |
|----|------|
| 维度 | 对比维度（子系列数、型号数、AI 功能覆盖、补光方式覆盖等） |
| Hikvision | 数值或列表 |
| Dahua | 数值或列表 |
| 差异说明 | 覆盖度差异描述 |

#### 7.3.6 报告元数据

| 字段 | 说明 |
|------|------|
| `analysis_report_meta` | 报告元数据 |
| report_id | 报告唯一标识 |
| run_id | 关联的数据采集批次 |
| generated_at | 生成时间 |
| hikvision_model_count | Hikvision 参与分析型号数 |
| dahua_model_count | Dahua 参与分析型号数 |
| mapping_count | 对标关系数 |
| diff_count | 差异记录数 |
| output_path | 报告文件路径 |

---

## 8. 数据模型与输出格式

### 8.1 层级结构 JSON（structure.json）

```json
{
  "generated_at": "ISO 时间戳",
  "series": {
    "Pro": {
      "EasyIP 4.0 with ColorVu": {
        "series_l1": "Pro",
        "subseries": "EasyIP 4.0 with ColorVu",
        "models_count": 66,
        "models": [
          { "model": "DS-2CD2xxx", "name": "...", "url": "..." }
        ]
      }
    },
    "Value": { "..." : "..." },
    "HiLook": { "..." : "..." }
  }
}
```

### 8.2 长表 CSV（specs_long.csv）

| 列 | 类型 | 说明 | 示例 |
|----|------|------|------|
| brand | string | 品牌 | `"hikvision"` |
| series_l1 | string | 一级系列 | `"Pro"` |
| subseries | string | 子系列 | `"EasyIP 4.0 with ColorVu"` |
| model | string | 产品型号 | `"DS-2CD2T47G2H-LISU/SL"` |
| name | string | 产品名称 | `"4MP..."` |
| url | string | 详情页 URL | `"https://..."` |
| field | string | 参数字段名 | `"Image Sensor"` |
| value | string | 参数值 | `"1/2.8\" CMOS"` |

### 8.3 宽表 CSV（specs_wide.csv）

- 6 列元数据：`brand, series_l1, series_l2, model, name, url`
- N 列参数字段（所有出现过的 field 去重排序）
- 同型号同字段多值时用换行符 `\n` 拼接

### 8.4 跨品牌对标关系（model_mapping）

详见 [7.1.4 输出](#714-输出)。

### 8.5 参数差异记录（param_diff）

详见 [7.2.4 输出](#724-输出)。

### 8.6 输出目录结构

```
results/
├── hikvision_specs_all_<runId>/
│   ├── specs_long.csv          # 长表（13MB, ~45,000 行）
│   ├── specs_long.jsonl        # JSONL 格式
│   ├── specs_wide.csv          # 宽表（694 型号 × 140 参数列）
│   ├── models.json             # 任务列表
│   ├── progress.json           # 进度信息
│   ├── run_meta.json           # 运行元信息
│   ├── done_urls.txt           # 已完成 URL（断点续爬）
│   └── errors.log              # 错误日志
├── dahua_specs_all_<runId>/
│   ├── specs_long.csv          # 长表（5MB, ~15,000 行）
│   ├── specs_long.jsonl        # JSONL 格式
│   ├── specs_wide.csv          # 宽表（191 型号 × 121 参数列）
│   ├── models.json
│   ├── progress.json
│   ├── run_meta.json
│   └── done_urls.txt
├── hikvision_structure_filtered_<runId>/
│   └── structure_filtered.json # Pro/Value/HiLook 层级结构
├── dahua_wizsense_structure_<runId>/
│   ├── dahua_wizsense_structure.json
│   ├── wizsense_2_page.html
│   └── wizsense_3_page.html
└── analysis_<runId>/
    ├── model_mapping.csv       # 跨品牌对标关系
    ├── param_diff.csv          # 参数差异记录
    ├── competitor_analysis.xlsx # 竞品分析 Excel 报告
    └── analysis_meta.json      # 分析运行元信息
```

---

## 9. 发现的技术问题与修复记录

### 9.1 Hikvision Pro 系列 AEM API 组件名不统一

**问题**：Pro 系列的 AEM 组件注册名为 `search_list_copy`，不是常规的 `search_list`。旧脚本假设所有系列 API 路径相同，直接拼接 `search_list.json`，导致 404 错误。

**修复**：改为从页面 HTML 中动态正则匹配 `/content/hikvision/.../responsivegrid/(search_list[^"']*)`，自动检测实际组件名。

**影响**：Pro 系列从 9 个子系列（含 1 个放错位置的）正确抓取到 8 个子系列，所有型号正确分组。

### 9.2 Dahua WizSense 2 硬编码 tab 过滤

**问题**：WizSense 2 的 `buildSeries()` 函数对 tab 做了硬编码过滤：

```javascript
tabs.filter((t) => {
  const l = t.toLowerCase();
  return l.includes("wizcolor") || l === "ir" || l.includes("smart dual light");
})
```

导致 "4G Camera" tab 被遗漏，只有 "WizColor"、"Smart Dual Light"、"IR" 被抓取。

**修复**：删除过滤逻辑，改为 `const selectedTabs = tabs;` 保留所有发现的 tab。

**影响**：WizSense 2 从 **3 个子系列** 扩展到 **4 个子系列**（新增 4G Camera，2 个型号）。

### 9.3 Playwright Chromium 缺失

**问题**：沙箱环境中未预装 Playwright 浏览器二进制文件，首次运行时报错 `Executable doesn't exist`。

**修复**：运行 `npx playwright install chromium` 下载 Chromium + FFmpeg，发现 headless shell 下载超时后，手动指定 `executablePath` 指向完整 Chrome 浏览器。

**影响**：所有 Playwright 脚本可正常运行。

### 9.4 输出路径硬编码为 Linux

**问题**：所有脚本的 `outDir` 硬编码为 `/workspace/results/...`（Linux 路径），在 Windows 环境中无效。

**修复**：改为 `D:\\work\\auto-CompetitionAnalysis\\results\\...`。

**影响**：输出文件正确写入 Windows 文件系统。

### 9.5 Value 系列 fallback 变量名错误

**问题**：Value 系列的 API fallback 路径中写的是 `models` 而不是 `models: products`，引用未定义变量会导致运行时错误。

**修复**：修正为 `models: products`。

### 9.6 Chromium 子进程文件沙箱冲突

**问题**：Chrome 子进程尝试写入 system-level 日志（`debug.log`、`sgephook_chrome.exe.log`），被沙箱环境拦截。

**影响**：仅产生非致命沙箱警告，不影响抓取数据正确性，忽略即可。

---

## 10. 当前抓取结果（2026-05-19 最新运行）

### 10.1 Hikvision 批量规格抓取

| 指标 | 数值 |
|------|:----:|
| 目标型号 | 706 |
| 成功抓取 | 688（97.4%） |
| 零参数型号 | 8 |
| 错误型号 | 6 |
| 规格记录数 | 45,571 条 |
| 参数字段数 | 140 |
| 输出目录 | `results/hikvision_specs_all_20260519_032013/` |

### 10.2 Dahua WizSense 批量规格抓取

| 指标 | 数值 |
|------|:----:|
| 目标型号 | 192 |
| 成功抓取 | 192（100%） |
| 零参数型号 | 0 |
| 错误型号 | 0 |
| 规格记录数 | 14,757 条 |
| 参数字段数 | 121 |
| 输出目录 | `results/dahua_specs_all_20260519_032007/` |

### 10.3 宽表输出

| 品牌 | 型号数 | 参数列数 | 文件 |
|------|:-----:|:--------:|------|
| Hikvision | 694 | 140 | `results/hikvision_specs_all_20260519_032013/specs_wide.csv` |
| Dahua | 191 | 121 | `results/dahua_specs_all_20260519_032007/specs_wide.csv` |

---

## 11. 已知问题与待办

### 11.1 Hikvision

| 问题 | 状态 | 说明 |
|------|:----:|------|
| Pro 下混入 1 个 Value 系列型号 | 已知 | API 返回的 JSON 中 1 个型号的 `Subseries` 字段被标为 "Value Series with MD 2.0" |
| 6 个型号抓取错误 | 日志记录 | 多为 URL 包含特殊字符导致文件写入异常（如 `#EU`、`/SL`） |
| 8 个型号零参数 | 需排查 | 可能页面结构不同或加载超时 |
| Value 系列子系列变化 | 需关注 | 官网可能会新增/合并子系列 |

### 11.2 Dahua

| 问题 | 状态 | 说明 |
|------|:----:|------|
| 全部型号 100% 成功 | ✅ | 无已知问题 |
| WizSense 2 "4G Camera" | 已修复 | 之前被硬编码过滤遗漏 |

### 11.3 Phase 2 待办

| 待办 | 优先级 | 说明 |
|------|:------:|------|
| 实现跨品牌型号对标匹配算法 | 高 | 基于参数相似度的多维度评分 |
| 实现参数差异分析引擎 | 高 | 逐参数对比，标注差异类型 |
| 实现 Excel 竞品分析报告导出 | 高 | 5 个 Sheet 结构 |
| 对标关系人工确认界面 | 中 | 支持人工确认/拒绝对标关系 |
| 分析结果可视化 | 低 | 参数差异热力图、覆盖度雷达图 |

---

## 12. 执行流程（端到端）

### 12.1 完整流程（5 Phase）

```
Phase 1: 层级结构发现（Node.js Playwright）
  ├── 运行 hikvision_structure_by_filter.mjs
  │   └── 输出: structure_filtered.json (Pro/Value/HiLook)
  └── 运行 dahua_wizsense_structure.mjs
      └── 输出: dahua_wizsense_structure.json (WizSense 2/3)

Phase 2: 批量规格抓取（Node.js Playwright）
  ├── 运行 hikvision_batch_specs_from_structure.mjs
  │   ├── 基于 structure_filtered.json
  │   └── 输出: specs_long.csv + jsonl
  └── 运行 dahua_batch_specs_from_structure.mjs
      ├── 基于 dahua_wizsense_structure.json
      └── 输出: specs_long.csv + jsonl

Phase 3: 宽表生成（Node.js + Python）
  ├── 运行 normalize_specs.py (Hikvision)
  │   └── 输出: specs_wide.csv (694×140)
  └── 运行 normalize_specs.py (Dahua)
      └── 输出: specs_wide.csv (191×121)

Phase 4: 数据导入与标准化（Python Pipeline）
  ├── 导入 CSV/JSONL 到 SQLite
  ├── 字段标准化（别名映射、单位归一化）
  └── 数据质量检测

Phase 5: 竞品分析与报告生成（Python Pipeline）
  ├── 跨品牌型号对标匹配
  ├── 参数差异分析
  └── Excel 竞品分析报告导出
```

### 12.2 快速执行入口

```bash
# Phase 1-3 一键执行（数据采集）
node run.mjs all

# 仅执行 Phase 1（层级发现）
node run.mjs discover:hikvision
node run.mjs discover:dahua

# 仅执行 Phase 2（规格抓取）
node run.mjs specs:hikvision
node run.mjs specs:dahua

# 仅执行 Phase 3（宽表生成）
node run.mjs wide:hikvision
node run.mjs wide:dahua

# Phase 4-5（Python Pipeline 分析）
python -c "from src.pipeline import run_manual_pipeline; run_manual_pipeline(...)"
```

---

## 13. 交付物清单

### 13.1 Phase 1 数据采集交付物

| 交付物 | 路径 | 说明 |
|--------|------|------|
| Hikvision 长表 | `results/hikvision_specs_all_20260519_032013/specs_long.csv` | 45,571 行规格记录 |
| Hikvision 宽表 | `results/hikvision_specs_all_20260519_032013/specs_wide.csv` | 694 型号 × 146 列 |
| Dahua 长表 | `results/dahua_specs_all_20260519_032007/specs_long.csv` | 14,757 行规格记录 |
| Dahua 宽表 | `results/dahua_specs_all_20260519_032007/specs_wide.csv` | 191 型号 × 127 列 |
| Hikvision 层级结构 | `results/hikvision_structure_filtered_20260519_022954/structure_filtered.json` | Pro 8 + Value 4 + HiLook 1 |
| Dahua 层级结构 | `results/dahua_wizsense_structure_20260519_022206/dahua_wizsense_structure.json` | WizSense 2 子系列 4 + WizSense 3 子系列 7 |
| 统一 CLI 入口 | `run.mjs` + `run.bat` | 8 个子命令，全流程一键执行 |

### 13.2 Phase 2 竞品分析交付物（规划）

| 交付物 | 路径 | 说明 |
|--------|------|------|
| 对标关系表 | `results/analysis_<runId>/model_mapping.csv` | Hikvision ↔ Dahua 型号对标关系 |
| 参数差异表 | `results/analysis_<runId>/param_diff.csv` | 逐参数差异记录 |
| 竞品分析报告 | `results/analysis_<runId>/competitor_analysis.xlsx` | Excel 多 Sheet 分析报告 |
| 分析元信息 | `results/analysis_<runId>/analysis_meta.json` | 分析运行统计 |

### 13.3 文档交付物

| 交付物 | 路径 | 说明 |
|--------|------|------|
| 需求文档 v2.0 | `docs/需求文档_竞品参数抓取系统_v2.0.md` | 本文档 |
| 需求文档 v1.0 | `docs/需求文档_竞品参数抓取系统_v1.0.md` | 前序版本（仅数据采集阶段） |
| PRD v0.7 | `docs/PRD_v0.7_竞品参数抓取与整理.md` | 产品需求文档 |
| 字段字典 v1 | `docs/field_dictionary_v1.md` | 冻结契约：12+ 必需字段 |

---

## 14. 脚本环境依赖

### 14.1 Phase 1 数据采集环境

| 依赖 | 版本 | 用途 |
|------|------|------|
| Node.js | >= 18 | 运行抓取脚本 |
| playwright | ^1.60.0 | 浏览器自动化 |
| Python | >= 3.11 | 宽表转换（normalize_specs.py） |
| Git | - | 版本控制 |

### 14.2 Phase 2 分析环境

| 依赖 | 版本 | 用途 |
|------|------|------|
| Python | >= 3.11 | 分析层核心 |
| SQLAlchemy | - | ORM |
| pandas | - | 数据处理 |
| openpyxl | - | Excel 导出 |
| lxml / beautifulsoup4 | - | HTML 解析 |
| httpx | - | HTTP 客户端 |
| playwright (Python) | - | 动态页面回退 |

### 14.3 环境路径

| 项目 | 路径 |
|------|------|
| Playwright 浏览器 | `C:\Users\<user>\AppData\Local\ms-playwright\chromium-1223\chrome-win64\chrome.exe` |
| Git | `D:\Program Files\Git\bin\git.exe` |
| Node.js 版本 | 22.17.1 |

---

## 15. 后续规划

| 阶段 | 内容 | 状态 |
|------|------|:----:|
| Phase 1 | 基础抓取 + 宽表输出 | ✅ 已完成 |
| Phase 2 | 竞品分析（型号对标 + 差异分析 + 报告生成） | 🔄 规划中 |
| Phase 3 | 跨品牌深度分析（系列布局对比、产品定位策略） | 📋 待规划 |
| Phase 4 | 大模型辅助分析与趋势报告 | 📋 待规划 |
